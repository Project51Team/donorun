name: Pull request CI
on:
  pull_request:
    types:
      - opened
      - synchronize
    branches-ignore:
      - 'master'
      - 'release/**'
jobs:
  pipeline:
    name: 'Build & Test'
    uses: './.github/workflows/build-test.yml'
